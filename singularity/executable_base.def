BootStrap: docker
From: ubuntu:16.04

%environment
    export DEBCONF_NOWARNINGS=yes

%post
    apt-get update
    apt-get upgrade -y
    apt-get install --no-install-recommends -y \
        libgmp3-dev \
        libmpfr-dev
    rm -rf /var/lib/apt/lists/*